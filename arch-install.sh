#!/bin/bash
# ==============================================================================
# arch-install.sh — 兼容壳
#
# 脚本已经重构成「install.sh 主控 + scripts/ 模块」的结构（照 shorin 那套）。
# 这里是给老习惯留的转发入口，直接跑 ./arch-install.sh 和 ./install.sh 一回事。
# ==============================================================================
exec "$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)/install.sh" "$@"
